# Repo mencakup seluruh project: web/ (Next.js), apps-script/ (backend GAS), PRD.
# Pengecualian internal web/ (node_modules, .next, .env.local) diatur oleh
# web/.gitignore. File ini menangani level root.

# Konfigurasi lokal Claude Code (per-mesin, jangan di-commit)
.claude/settings.local.json

# Rahasia (jaga-jaga bila ada .env di root)
.env
.env.*
!.env*.example

# OS / editor
.DS_Store
Thumbs.db
*.swp

# Supabase temporary files
supabase/.temp/
